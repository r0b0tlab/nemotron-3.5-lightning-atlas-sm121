# Privacy

This repository must not contain:

- model weights or tokenizer blobs
- API tokens, PATs, or `.env` files
- private hostnames, RFC1918 addresses, or local usernames
- raw server logs with machine identifiers

Evidence JSON in `evidence/` is protocol measurements only.
