# WIP / not a qualified release

This is a sanitized, selected-lane evidence snapshot for collaboration. The source/runtime candidate remains work in progress. The r0b0bench run used `--only latency,concurrency,throughput,gsm8k`, so it is intentionally `invalid_for_publish=true` and must not be represented as a full public core-subset result. Model weights, prompts, responses, raw host telemetry, credentials, host paths, addresses, PIDs, container IDs, and device pointers are excluded.
